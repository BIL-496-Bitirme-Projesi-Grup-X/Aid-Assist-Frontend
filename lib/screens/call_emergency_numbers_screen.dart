import 'package:flutter/material.dart';

class CallEmergencyNumbersScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text("ALO YARDIM")),
        body: Center(
          child: Text("ALO YARDIM"),
        ));
  }
}
